MINIO_VOLUMES= "{% for item in groups['minio_Servers'] -%} http://{{ item }}:9000{{ minio_storage_path }}/minio {% if not loop.last %} {% endif %} {%- endfor %}"

MINIO_OPTS="--console-address :9001  {{ minio_server_opts }}"


{% if minio_access_key %}
# Access Key of the server.
MINIO_ROOT_USER="{{ minio_access_key }}"
{% endif %}
{% if minio_secret_key %}
# Secret key of the server.
MINIO_ROOT_PASSWORD="{{ minio_secret_key }}"
{% endif %}

{{ minio_server_env_extra }}